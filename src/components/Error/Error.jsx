import { Flex } from '@chakra-ui/react';

export const Error = () => {
  return (
    <Flex bg={'var(--background)'} h={'100vh'} justify={'center'} color={'var(--font-color)'}>
      <h1>Errors happens</h1>
    </Flex>
  );
};
